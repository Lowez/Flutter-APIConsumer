import 'package:api_consume/screens/home.dart';
import 'package:flutter/material.dart';

void main() {
  runApp(const MaterialApp(
      title: 'Consumo de APIs',
      home: Home(),
      debugShowCheckedModeBanner: false,
    ),);
}
